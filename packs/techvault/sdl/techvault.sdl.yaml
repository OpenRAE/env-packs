name: techvault
description: >
  Operational RAES projection for the TechVault range. This SDL is the public
  startup contract: it names the steady-state Compose services and networks
  that `aptl lab start` realizes. There is no separate captured deep
  per-asset inventory behind it (ADR-046).

nodes:
  security-net:
    type: switch
    description: SOC and security tooling network.
  dmz-net:
    type: switch
    description: TechVault DMZ network.
  internal-net:
    type: switch
    description: Internal enterprise target network.
  redteam-net:
    type: switch
    description: Red-team operations network.
  control-net:
    type: switch
    description: >-
      Control network carrying operator access to the range: the Kali SSH proxy
      and the web application proxy attach here alongside their range-facing
      networks. Not internal, so the host can reach the published proxy ports.

  wazuh-manager:
    type: vm
    os: linux
    source:
      name: "wazuh/wazuh-manager"
      version: "4.12.0"
      artifact_requirement:
        requirement_id: wazuh-manager-image
        explicitness: exact
        exact_artifact:
          artifact_id: "wazuh/wazuh-manager"
          version: "4.12.0"
          digest: "sha256:dea2fa1e6d5062147b6a85b241f5f501c5f1ba4b817d12bda06f7870a89ad561"
          media_type: "application/vnd.docker.distribution.manifest.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: wazuh-api, port: 55000, protocol: tcp}
      - {name: agent-events, port: 1514, protocol: tcp}
      - {name: agent-enrollment, port: 1515, protocol: tcp}
      - {name: syslog, port: 514, protocol: udp}
    runtime:
      security_monitoring_managers:
        - security_monitoring_manager_id: wazuh-manager
          implementation: wazuh
          manager_kind: siem
          version: 4.12.0
          name: TechVault SIEM
          description: The scenario's SIEM. Agents enroll here, ship events here, and the analysis engine evaluates them against the rule corpora below.
          listeners:
            - listener_id: agent-events
              service: agent-events
              role: agent_event_ingestion
              protocol: tcp
              auth_required: true
              tls_enabled: false
            - listener_id: agent-enrollment
              service: agent-enrollment
              role: agent_enrollment
              protocol: tcp
              auth_required: true
              tls_enabled: true
            - listener_id: syslog
              service: syslog
              role: syslog_ingestion
              protocol: udp
              auth_required: false
              tls_enabled: false
            - listener_id: wazuh-api
              service: wazuh-api
              role: api
              protocol: tcp
              auth_required: true
              tls_enabled: true
          components:
            - component_id: analysisd
              kind: analysis_engine
              name: wazuh-analysisd
              enabled: true
            - component_id: remoted
              kind: agent_ingestion
              name: wazuh-remoted
              enabled: true
            - component_id: authd
              kind: agent_enrollment
              name: wazuh-authd
              enabled: true
            - component_id: apid
              kind: api
              name: wazuh-apid
              enabled: true
            - component_id: indexer-forwarder
              kind: indexer_forwarder
              name: filebeat
              enabled: true
          agents:
            - agent_id: suricata-agent
              name: aptl-suricata-agent
              node_ref: wazuh-sidecar-suricata
              description: Ships Suricata EVE events; see that node's forwarding agent.
            - agent_id: db-agent
              name: aptl-db-agent
              node_ref: wazuh-sidecar-db
              description: Ships PostgreSQL logs; see that node's forwarding agent.
          content_sets:
            - content_id: webapp-rules
              kind: rule_corpus
              format: wazuh_rule_xml
              name: webapp_rules.xml
              file_count: 11
              loaded: true
              description: Web-application attack rules, including the SQL-injection detections the range exercises.
            - content_id: suricata-rules
              kind: rule_corpus
              format: wazuh_rule_xml
              name: suricata_rules.xml
              file_count: 12
              loaded: true
              description: Rules that fire on Suricata EVE events arriving from the network sensor.
            - content_id: ad-rules
              kind: rule_corpus
              format: wazuh_rule_xml
              name: ad_rules.xml
              file_count: 11
              loaded: true
              description: Active Directory authentication and abuse rules.
            - content_id: database-rules
              kind: rule_corpus
              format: wazuh_rule_xml
              name: database_rules.xml
              file_count: 7
              loaded: true
              description: Customer-database access and abuse rules.
            - content_id: falco-rules
              kind: rule_corpus
              format: wazuh_rule_xml
              name: falco_rules.xml
              file_count: 8
              loaded: true
              description: Container runtime rules.
            - content_id: postgresql-decoders
              kind: decoder_corpus
              format: wazuh_decoder_xml
              name: postgresql_decoders.xml
              file_count: 2
              loaded: true
              description: Decoders that make PostgreSQL log lines parseable by the rules.
            - content_id: samba-decoders
              kind: decoder_corpus
              format: wazuh_decoder_xml
              name: samba_decoders.xml
              file_count: 2
              loaded: true
              description: Decoders for Samba file-share activity.
          detection_definitions:
            - definition_id: sqli-special-characters
              engine: wazuh
              definition_kind: rule
              native_id: '302011'
              content_set_ref: webapp-rules
              level: 8
              enabled: true
              loaded: true
              name: 'Potential SQL injection: special characters in URL'
              description: The detection the range's SQL-injection path is expected to trigger. Named here because the scenario depends on it firing, not merely on the corpus being loaded.
              groups:
                - webapp
                - sqli
                - web_attack
            - definition_id: sqli-union-select
              engine: wazuh
              definition_kind: rule
              native_id: '302010'
              content_set_ref: webapp-rules
              level: 10
              enabled: true
              loaded: true
              name: SQL injection attempt detected in URL
              groups:
                - webapp
                - sqli
                - web_attack
  wazuh-indexer:
    type: vm
    os: linux
    source:
      name: "wazuh/wazuh-indexer"
      version: "4.12.0"
      artifact_requirement:
        requirement_id: wazuh-indexer-image
        explicitness: exact
        exact_artifact:
          artifact_id: "wazuh/wazuh-indexer"
          version: "4.12.0"
          digest: "sha256:3691b3b27658695aad0c6879b412a001caf233ebbc1a5ba15647053aa03a2299"
          media_type: "application/vnd.docker.distribution.manifest.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: indexer-api, port: 9200, protocol: tcp}
    runtime:
      datastore_services:
        - datastore_service_id: wazuh-indexer
          engine: opensearch
          data_model: search_index
          version: 4.12.0
          description: Alert store for the SIEM. Alert indices are created on first ingest; the templates that shape them exist from boot.
          partitions:
            - partition_id: kibana
              kind: index
              name: .kibana_1
              shard_count: 1
              replica_count: 0
            - partition_id: security
              kind: index
              name: .opendistro_security
              shard_count: 1
              replica_count: 0
            - partition_id: observability
              kind: index
              name: .opensearch-observability
              shard_count: 1
              replica_count: 0
            - partition_id: ml-config
              kind: index
              name: .plugins-ml-config
              shard_count: 1
              replica_count: 0
          templates:
            - template_id: wazuh-agent
              name: wazuh-agent
            - template_id: wazuh-statistics
              name: wazuh-statistics
          mappings:
            - mapping_id: kibana-objects
              name: .kibana_1
              partition_ref: kibana
              top_level_field_count: 28
              description: Saved-object mapping for the dashboard tier.
  wazuh-dashboard:
    type: vm
    os: linux
    source:
      name: "wazuh/wazuh-dashboard"
      version: "4.12.0"
      artifact_requirement:
        requirement_id: wazuh-dashboard-image
        explicitness: exact
        exact_artifact:
          artifact_id: "wazuh/wazuh-dashboard"
          version: "4.12.0"
          digest: "sha256:8f5b50fde67a0b1c4d2321aa26b12bbc5cef21269cf4f6225746f0b946458bd7"
          media_type: "application/vnd.docker.distribution.manifest.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: dashboard, port: 5601, protocol: tcp}

  suricata:
    type: vm
    os: linux
    source:
      name: "jasonish/suricata"
      version: "sha256:d64c491f6eb5d03b6562d873b3c5303e4c17d5dc1d75b3761a68319f11527dc3"
      artifact_requirement:
        requirement_id: suricata-image
        explicitness: exact
        exact_artifact:
          artifact_id: "jasonish/suricata"
          version: "sha256:d64c491f6eb5d03b6562d873b3c5303e4c17d5dc1d75b3761a68319f11527dc3"
          digest: "sha256:d64c491f6eb5d03b6562d873b3c5303e4c17d5dc1d75b3761a68319f11527dc3"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services: []
    runtime:
      network_sensors:
        - network_sensor_id: suricata-sensor
          implementation: suricata
          sensor_kind: ids
          monitoring_posture: passive
          capture_mode: af_packet
          capture_interfaces:
            - any
          monitored_network_refs:
            - security-net
            - dmz-net
            - internal-net
            - redteam-net
          name: TechVault network sensor
          description: Watches range traffic passively. Its EVE output is what the Wazuh sidecar tails, so this is where network visibility enters the SOC.
      network_detection_engines:
        - network_detection_engine_id: suricata-engine
          implementation: suricata
          engine_kind: ids
          sensor_ref: suricata-sensor
          name: TechVault detection engine
          description: Evaluates captured traffic against its rule sources and writes EVE events. MISP-derived rules are reloaded here over the command socket.
          app_layer_protocols:
            - http
            - tls
            - dns
            - ssh
            - smb
            - smtp
            - ftp
          rule_sources:
            - source_id: suricata-builtin
              kind: built_in
              format: suricata_rule
              name: suricata.rules
              loaded: true
              description: Ruleset shipped with the engine image.
            - source_id: techvault-local
              kind: local
              format: suricata_rule
              name: local.rules
              rule_count: 16
              loaded: true
              description: Scenario-authored rules for TechVault-specific activity.
            - source_id: misp-iocs
              kind: ioc
              format: suricata_rule
              name: misp/misp-iocs.rules
              loaded: true
              generated_by: nodes.misp-suricata-sync.runtime.forwarding_agents.misp-ioc-to-suricata
              description: Written by the MISP sync agent, not authored here. This is the threat-intelligence feed becoming live detection content.
          network_sets:
            - set_id: home-net
              kind: home_net
              name: HOME_NET
              selector_values:
                - 172.20.0.0/16
              network_refs:
                - security-net
                - dmz-net
                - internal-net
                - redteam-net
                - control-net
              description: The range itself; everything outside it is EXTERNAL_NET.
          output_streams:
            - stream_id: eve-json
              format: eve_json
              path: /var/log/suricata/eve.json
              event_types:
                - alert
                - http
                - dns
                - tls
                - ssh
                - flow
                - stats
              enabled: true
              description: The file the Wazuh sidecar tails off the shared suricata_logs volume. This path is the detection-to-SIEM hand-off.
          control_channels:
            - channel_id: command-socket
              kind: unix_socket
              path: /var/run/suricata/suricata-command.socket
              capabilities:
                - rule_reload
              auth_required: false
              description: Where the MISP sync agent drives a reload after writing rules.
  misp:
    type: vm
    os: linux
    source:
      name: "ghcr.io/misp/misp-docker/misp-core"
      version: "sha256:0eaa4e423d5cd965b7b76aa5665e81d5c05a35bc46a4ffec2ca52e0cfe627e86"
      artifact_requirement:
        requirement_id: misp-image
        explicitness: exact
        exact_artifact:
          artifact_id: "ghcr.io/misp/misp-docker/misp-core"
          version: "sha256:0eaa4e423d5cd965b7b76aa5665e81d5c05a35bc46a4ffec2ca52e0cfe627e86"
          digest: "sha256:0eaa4e423d5cd965b7b76aa5665e81d5c05a35bc46a4ffec2ca52e0cfe627e86"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: https, port: 443, protocol: tcp}
    runtime: {}
  misp-db:
    type: vm
    os: linux
    source:
      name: "mariadb"
      version: "sha256:be981e4113326ada8d6004174dd09eeaefc03094037f811182a52d4f2e737350"
      artifact_requirement:
        requirement_id: misp-db-image
        explicitness: exact
        exact_artifact:
          artifact_id: "mariadb"
          version: "sha256:be981e4113326ada8d6004174dd09eeaefc03094037f811182a52d4f2e737350"
          digest: "sha256:be981e4113326ada8d6004174dd09eeaefc03094037f811182a52d4f2e737350"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: mysql, port: 3306, protocol: tcp}
  misp-redis:
    type: vm
    os: linux
    source:
      name: "redis"
      version: "sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99"
      artifact_requirement:
        requirement_id: misp-redis-image
        explicitness: exact
        exact_artifact:
          artifact_id: "redis"
          version: "sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99"
          digest: "sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: redis, port: 6379, protocol: tcp}
  misp-suricata-sync:
    type: vm
    os: linux
    services: []
    runtime:
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: MISP_URL
          value: "https://misp"
          value_classification: plain
          provenance: compose
        - name: MISP_API_KEY
          value_classification: operator_secret
          provenance: compose
        - name: MISP_VERIFY_SSL
          value_classification: plain
          provenance: compose
        - name: MISP_CA_CERT_PATH
          value_classification: plain
          provenance: compose
        - name: IOC_TAG_FILTER
          value: "aptl:enforce"
          value_classification: plain
          provenance: compose
        - name: SYNC_INTERVAL_SECONDS
          value: "300"
          value_classification: plain
          provenance: compose
        - name: RULES_OUT_PATH
          value: "/var/lib/suricata/rules/misp/misp-iocs.rules"
          value_classification: plain
          provenance: compose
        - name: SURICATA_SOCKET_PATH
          value: "/var/run/suricata/suricata-command.socket"
          value_classification: plain
          provenance: compose
        - name: SID_BASE
          value: "99000000"
          value_classification: plain
          provenance: compose
        - name: LOG_LEVEL
          value: "INFO"
          value_classification: plain
          provenance: compose
      packages:
        - {manager: apt, name: python3-pip, version: "*"}
      dependency_manifests:
        - {ecosystem: pip, path: /app/pyproject.toml, name: aptl-labs}
      # Shares suricata's rules volume (ADR-043): this service writes
      # misp-iocs.rules here for suricata to load, and the command socket to
      # trigger its reload. Both are Compose-managed named volumes suricata
      # (still Compose-realized) already mounts at the same paths.
      mounts:
        - {target: /var/run/suricata, source: suricata_command_socket, source_kind: volume}
        - {target: /var/lib/suricata/rules/misp, source: suricata_misp_rules, source_kind: volume}
      forwarding_agents:
        - forwarding_agent_id: misp-ioc-to-suricata
          implementation: misp_suricata_sync
          agent_kind: content_sync
          description: Pulls tagged indicators from MISP, renders them into Suricata rules, and drives Suricata's command socket to reload. This is how threat intelligence becomes active network detection content.
          sources:
            - source_id: misp-attributes
              kind: api_pull
              location: https://misp
              parse_format: misp_json
              selector: aptl:enforce
          transforms:
            - transform_id: ioc-to-suricata-rules
              kind: ioc_to_rule
              sid_namespace: '3000000'
          reload_channels:
            - reload_channel_id: suricata-command-socket
              target_ref: suricata
              kind: unix_socket
  thehive:
    type: vm
    os: linux
    source:
      name: "strangebee/thehive"
      version: "sha256:ba3212a89be79de6ec8e6e66b84f3c0801c3b8d726aacc767ad6257030df7a13"
      artifact_requirement:
        requirement_id: thehive-image
        explicitness: exact
        exact_artifact:
          artifact_id: "strangebee/thehive"
          version: "sha256:ba3212a89be79de6ec8e6e66b84f3c0801c3b8d726aacc767ad6257030df7a13"
          digest: "sha256:ba3212a89be79de6ec8e6e66b84f3c0801c3b8d726aacc767ad6257030df7a13"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: thehive-api, port: 9000, protocol: tcp}
  thehive-cassandra:
    type: vm
    os: linux
    source:
      name: "cassandra"
      version: "sha256:d25e8ee78d648fade002d0d176b7e8c953c69164b9f316ccbccf62f524c9dfbf"
      artifact_requirement:
        requirement_id: thehive-cassandra-image
        explicitness: exact
        exact_artifact:
          artifact_id: "cassandra"
          version: "sha256:d25e8ee78d648fade002d0d176b7e8c953c69164b9f316ccbccf62f524c9dfbf"
          digest: "sha256:d25e8ee78d648fade002d0d176b7e8c953c69164b9f316ccbccf62f524c9dfbf"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: cassandra, port: 9042, protocol: tcp}
    runtime:
      datastore_services:
        - datastore_service_id: thehive-cassandra
          engine: cassandra
          data_model: wide_column
          version: '4.1'
          description: Primary case store.
          partitions:
            - partition_id: thehive
              kind: keyspace
              name: thehive
              replication_strategy: simple_strategy
              replication_factor: 1
  thehive-es:
    type: vm
    os: linux
    source:
      name: "docker.elastic.co/elasticsearch/elasticsearch"
      version: "sha256:f2ce8a4c644a35762e6e115c9a373c5cd20df03c2dd75cb0a570011934cdffd1"
      artifact_requirement:
        requirement_id: thehive-es-image
        explicitness: exact
        exact_artifact:
          artifact_id: "docker.elastic.co/elasticsearch/elasticsearch"
          version: "sha256:f2ce8a4c644a35762e6e115c9a373c5cd20df03c2dd75cb0a570011934cdffd1"
          digest: "sha256:f2ce8a4c644a35762e6e115c9a373c5cd20df03c2dd75cb0a570011934cdffd1"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: elasticsearch, port: 9200, protocol: tcp}
    runtime:
      datastore_services:
        - datastore_service_id: thehive-es
          engine: elasticsearch
          data_model: search_index
          version: 7.17.2
          description: Search backend for case management, and host of the Cortex job index.
          partitions:
            - partition_id: cortex
              kind: index
              name: cortex_6
              shard_count: 1
              replica_count: 1
            - partition_id: geoip
              kind: index
              name: .geoip_databases
              shard_count: 1
              replica_count: 0
          mappings:
            - mapping_id: cortex-jobs
              name: cortex_6
              partition_ref: cortex
              top_level_field_count: 3
              description: Job index mapping pre-created so key, relations, and status stay keyword-typed.
  cortex:
    type: vm
    os: linux
    source:
      name: "thehiveproject/cortex"
      version: "sha256:ae8b3d72eb5de785513bc33492d93278c32b79d9ff89401463c3a9c577e0bc0b"
      artifact_requirement:
        requirement_id: cortex-image
        explicitness: exact
        exact_artifact:
          artifact_id: "thehiveproject/cortex"
          version: "sha256:ae8b3d72eb5de785513bc33492d93278c32b79d9ff89401463c3a9c577e0bc0b"
          digest: "sha256:ae8b3d72eb5de785513bc33492d93278c32b79d9ff89401463c3a9c577e0bc0b"
          media_type: "application/vnd.docker.distribution.manifest.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: cortex-api, port: 9001, protocol: tcp}
  shuffle-backend:
    type: vm
    os: linux
    source:
      name: "ghcr.io/shuffle/shuffle-backend"
      version: "sha256:d4a5d2bf1f956955b68b099ba1c38997e4b257b2518215e0427f433515bea5c8"
      artifact_requirement:
        requirement_id: shuffle-backend-image
        explicitness: exact
        exact_artifact:
          artifact_id: "ghcr.io/shuffle/shuffle-backend"
          version: "sha256:d4a5d2bf1f956955b68b099ba1c38997e4b257b2518215e0427f433515bea5c8"
          digest: "sha256:d4a5d2bf1f956955b68b099ba1c38997e4b257b2518215e0427f433515bea5c8"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: shuffle-api, port: 5001, protocol: tcp}
  shuffle-frontend:
    type: vm
    os: linux
    source:
      name: "ghcr.io/shuffle/shuffle-frontend"
      version: "sha256:4d700a6f0822cb081822bd2fa6c633080553bdd4313aed2c4bdce75b87e82836"
      artifact_requirement:
        requirement_id: shuffle-frontend-image
        explicitness: exact
        exact_artifact:
          artifact_id: "ghcr.io/shuffle/shuffle-frontend"
          version: "sha256:4d700a6f0822cb081822bd2fa6c633080553bdd4313aed2c4bdce75b87e82836"
          digest: "sha256:4d700a6f0822cb081822bd2fa6c633080553bdd4313aed2c4bdce75b87e82836"
          media_type: "application/vnd.oci.image.index.v1+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: https, port: 443, protocol: tcp}
      - {name: http, port: 80, protocol: tcp}
  shuffle-orborus:
    type: vm
    os: linux
    source:
      name: "ghcr.io/shuffle/shuffle-orborus"
      version: "sha256:94e61e7916aea28351fce3851f26f14fb85204f1567a8807d137321418366dba"
      artifact_requirement:
        requirement_id: shuffle-orborus-image
        explicitness: exact
        exact_artifact:
          artifact_id: "ghcr.io/shuffle/shuffle-orborus"
          version: "sha256:94e61e7916aea28351fce3851f26f14fb85204f1567a8807d137321418366dba"
          digest: "sha256:94e61e7916aea28351fce3851f26f14fb85204f1567a8807d137321418366dba"
          media_type: "application/vnd.docker.distribution.manifest.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services: []
    runtime:
      orchestration_authorities:
        - orchestration_authority_id: shuffle-orborus
          engine: docker
          name: Shuffle worker orchestrator
          privilege_class: host_root_equivalent
          description: Spawns SOAR worker containers on demand. It mounts the host Docker socket, so it can create workloads outside the declared node set; that authority is stated here rather than left implicit.
          spawn_templates:
            - template_id: shuffle-worker
              image_ref: ghcr.io/shuffle/shuffle-worker:latest
              purpose: workflow execution
          lifecycle_policy:
            execution_timeout: '600'
            description: Per-execution ceiling from SHUFFLE_ORBORUS_EXECUTION_TIMEOUT.
          control_interface_ref: docker-sock
      local_control_interfaces:
        - control_interface_id: docker-sock
          path: /var/run/docker.sock
          kind: unix_socket
          access: read_write
          bind_source: /var/run/docker.sock
          description: The host Docker socket, mounted read-write. Holding it is what lets this node create containers outside the declared range.
  shuffle-opensearch:
    type: vm
    os: linux
    source:
      name: "opensearchproject/opensearch"
      version: "sha256:466a49f379bb8889af29d615475e69b7b990898c6987d28470cd7105df9046ff"
      artifact_requirement:
        requirement_id: shuffle-opensearch-image
        explicitness: exact
        exact_artifact:
          artifact_id: "opensearchproject/opensearch"
          version: "sha256:466a49f379bb8889af29d615475e69b7b990898c6987d28470cd7105df9046ff"
          digest: "sha256:466a49f379bb8889af29d615475e69b7b990898c6987d28470cd7105df9046ff"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: opensearch-rest, port: 9200, protocol: tcp}
      - {name: opensearch-transport, port: 9300, protocol: tcp}
    runtime:
      datastore_services:
        - datastore_service_id: shuffle-opensearch
          engine: opensearch
          data_model: search_index
          version: 2.14.0
          description: Backing store for SOAR workflows, environments, and execution state.
          partitions:
            - partition_id: workflowqueue
              kind: index
              name: workflowqueue-shuffle
              shard_count: 1
              replica_count: 1
            - partition_id: notifications
              kind: index
              name: notifications-000001
              shard_count: 3
              replica_count: 1
            - partition_id: platform_health
              kind: index
              name: platform_health
              shard_count: 1
              replica_count: 1
            - partition_id: workflowexecution
              kind: index
              name: workflowexecution-000001
              shard_count: 3
              replica_count: 1
            - partition_id: environments
              kind: index
              name: environments-000001
              shard_count: 3
              replica_count: 1
            - partition_id: workflowapp
              kind: index
              name: workflowapp-000001
              shard_count: 3
              replica_count: 1
            - partition_id: users
              kind: index
              name: users
              shard_count: 1
              replica_count: 1
            - partition_id: shuffle_logs
              kind: index
              name: shuffle_logs-000001
              shard_count: 3
              replica_count: 1
            - partition_id: datastore_ngram
              kind: index
              name: datastore_ngram-000001
              shard_count: 3
              replica_count: 1
            - partition_id: openapi3
              kind: index
              name: openapi3
              shard_count: 1
              replica_count: 1
          mappings:
            - mapping_id: users-mapping
              name: users
              partition_ref: users
              top_level_field_count: 30
              description: SOAR user record mapping.
  wazuh-sidecar-db:
    type: vm
    os: linux
    source:
      name: "aptl-wazuh-sidecar"
      version: local
      artifact_requirement:
        requirement_id: wazuh-sidecar-db-image
        explicitness: constrained
        materialization_specifications:
          - specification_id: wazuh-sidecar
            profile:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            digest: "sha256:1fdafc28ba63364e7d91a5fbb68bce8e251f35b40287ea99a7d3c903198a8985"
            locked_input_ids: []
        permitted_routes:
          - mechanism:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            acquisition: none
            timing: backend-preparation
    services: []
    runtime:
      forwarding_agents:
        - forwarding_agent_id: db-postgres-forwarder
          implementation: wazuh_agent
          agent_kind: log_forwarder
          name: aptl-db-agent
          description: Tails the customer database's PostgreSQL log off the shared db_data volume and ships it to the Wazuh manager, so database activity reaches the SIEM.
          sources:
            - source_id: postgres-log
              kind: tailed_path
              location: /logs/pg_log/postgresql.log
              parse_format: syslog
          transforms:
            - transform_id: postgres-parse
              kind: parse
          buffer_policy:
            buffer_policy_id: db-postgres-forwarder-buffer
            crypto: aes
          ship_targets:
            - target_id: db-postgres-forwarder-manager
              target_node_ref: wazuh-manager
              ingestion_port: 1514
              enrollment_port: 1515
              protocol: tcp
              enrollment_identity_classification: operator_secret
  wazuh-sidecar-suricata:
    type: vm
    os: linux
    source:
      name: "aptl-wazuh-sidecar"
      version: local
      artifact_requirement:
        requirement_id: wazuh-sidecar-suricata-image
        explicitness: constrained
        materialization_specifications:
          - specification_id: wazuh-sidecar
            profile:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            digest: "sha256:1fdafc28ba63364e7d91a5fbb68bce8e251f35b40287ea99a7d3c903198a8985"
            locked_input_ids: []
        permitted_routes:
          - mechanism:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            acquisition: none
            timing: backend-preparation
    services: []
    runtime:
      forwarding_agents:
        - forwarding_agent_id: suricata-eve-forwarder
          implementation: wazuh_agent
          agent_kind: log_forwarder
          name: aptl-suricata-agent
          description: Tails Suricata's EVE output off the shared suricata_logs volume and ships it to the Wazuh manager. This is the hand-off that makes network detections visible to the SIEM.
          sources:
            - source_id: suricata-eve
              kind: tailed_path
              location: /logs/eve.json
              parse_format: eve_json
          transforms:
            - transform_id: eve-parse
              kind: parse
          buffer_policy:
            buffer_policy_id: suricata-eve-forwarder-buffer
            crypto: aes
          ship_targets:
            - target_id: suricata-eve-forwarder-manager
              target_node_ref: wazuh-manager
              ingestion_port: 1514
              enrollment_port: 1515
              protocol: tcp
              enrollment_identity_classification: operator_secret

  webapp:
    type: vm
    os: linux
    services:
      - {name: http, port: 8080, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /var/log/gunicorn, source: webapp_logs, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: DB_HOST
          value: "172.20.2.11"
          value_classification: plain
          provenance: compose
        - name: DB_PORT
          value: "5432"
          value_classification: plain
          provenance: compose
        - name: DB_NAME
          value: "techvault"
          value_classification: plain
          provenance: compose
        - name: DB_USER
          value: "techvault"
          value_classification: plain
          provenance: compose
        - name: DB_PASSWORD
          value: "techvault_db_pass"
          value_classification: secret_fixture
          provenance: compose
        - name: FLASK_ENV
          value: "production"
          value_classification: plain
          provenance: compose
        - name: SIEM_IP
          value: "172.20.2.30"
          value_classification: plain
          provenance: compose
        - name: WAZUH_MANAGER
          value: "wazuh.manager"
          value_classification: plain
          provenance: compose
        - name: AGENT_NAME
          value: "aptl-webapp-agent"
          value_classification: plain
          provenance: compose
        - name: LOG_PATHS
          value: "/var/log/gunicorn/access.log"
          value_classification: plain
          provenance: compose
        - name: LOG_FORMAT
          value: "syslog"
          value_classification: plain
          provenance: compose
      packages:
        - {manager: apt, name: python3-flask, version: "*"}
        - {manager: apt, name: python3-gunicorn, version: "*"}
        - {manager: apt, name: python3-psycopg2, version: "*"}
        - {manager: apt, name: python3-jwt, version: "*"}
        # rsyslog forwards gunicorn's access log to the Wazuh manager's syslog
        # port (172.20.2.30:514) so webapp SQLi/XSS/cmd-injection fire rules
        # 302010/302020/302030. #581 parity: the v5.0.0 webapp ran an in-process
        # Wazuh agent for this; on the generic node we forward via syslog (the
        # manager accepts remote syslog on 514 from 172.20.0.0/16), which the
        # web-accesslog decoder + custom rules process identically.
        - {manager: apt, name: rsyslog, version: "*"}
        # aptl lab continuity-audit (src/aptl/core/continuity.py) execs
        # iptables here to revert blanket kali source-IP DROPs left by Wazuh
        # active response. The node already declares CAP_NET_ADMIN for that;
        # without the tool itself the audit cannot inspect or clear the chain.
        - {manager: apt, name: iptables, version: "*"}
      filesystem_inventory:
        - {path: /var/log/gunicorn, entry_type: directory}
      # aptl lab continuity-audit (src/aptl/core/continuity.py) execs
      # iptables against this node's INPUT chain to revert blanket kali
      # source-IP DROPs; that needs CAP_NET_ADMIN, which Docker's default
      # capability set does not grant.
      linux_capabilities:
        add: [CAP_NET_ADMIN]
      # webapp is a deliberate attack-surface target, reachable from other
      # machines on the LAN like the in-range red team (test_docker_compose_
      # port_bindings.py TARGET_SURFACES), not an operator/management
      # surface -- so it does NOT get a loopback ADR-034 published_ports
      # declaration here. Its own networks (aptl-dmz, aptl-internal) are
      # both internal: true (SAF-002), which Docker never routes host
      # traffic through regardless of host_ip; the docker-compose.yml
      # webapp-proxy service is the real host publication (issue #581).
      service_manager_units:
        - {unit_id: webapp, unit_name: webapp.service, enabled_state: enabled, active_state: active}
        # Per-boot CTF flags (#581 parity; see aptl-flaggen.sh content below).
        - {unit_id: aptl-flaggen, unit_name: aptl-flaggen.service, enabled_state: enabled, active_state: active}
        # Custom rsyslog unit (below): the stock rsyslog.service carries systemd
        # sandboxing (ProtectKernelTunables/RestrictNamespaces/...) that fails
        # 226/NAMESPACE under the materializer's restricted caps, and a drop-in
        # would need a daemon-reload the materializer does not run. A fresh unit
        # file loads on start (like webapp.service) with no sandboxing.
        - {unit_id: aptl-rsyslog, unit_name: aptl-rsyslog.service, enabled_state: enabled, active_state: active}
      applications:
        - application_id: techvault-portal
          service: http
          protocol: http
          name: TechVault customer portal
          framework: flask
          base_path: /
          description: The internet-facing business application. Its weaknesses are the scenario content an attacker is meant to find.
          routes:
            - route_id: index
              path: /
              methods:
                - GET
              auth_required: false
              description: Public landing page.
            - route_id: login
              path: /login
              methods:
                - GET
                - POST
              auth_required: false
              description: Login form.
              vulnerability_refs:
                - webapp-sqli-login
                - webapp-verbose-errors
            - route_id: logout
              path: /logout
              methods:
                - GET
              auth_required: true
              description: Ends the session.
            - route_id: dashboard
              path: /dashboard
              methods:
                - GET
              auth_required: true
              description: Authenticated user dashboard.
            - route_id: admin
              path: /admin
              methods:
                - GET
              auth_required: true
              description: Administrative view over stored backup configuration.
              vulnerability_refs:
                - webapp-missing-authz-admin
            - route_id: upload
              path: /upload
              methods:
                - GET
                - POST
              auth_required: true
              description: File upload.
              vulnerability_refs:
                - webapp-unrestricted-upload
            - route_id: api-file
              path: /api/v1/files/<int:file_id>
              methods:
                - GET
              auth_required: true
              description: Fetch one file record.
              vulnerability_refs:
                - webapp-idor-files
            - route_id: api-user
              path: /api/v1/users/<int:user_id>
              methods:
                - GET
              auth_required: true
              description: Fetch one user record.
              vulnerability_refs:
                - webapp-idor-users
            - route_id: api-customers
              path: /api/v1/customers
              methods:
                - GET
              auth_required: true
              description: List customers.
            - route_id: api-token
              path: /api/v1/token
              methods:
                - POST
              auth_required: false
              description: Issue a bearer token.
              vulnerability_refs:
                - webapp-weak-jwt
                - webapp-hardcoded-secrets
            - route_id: ping-tool
              path: /tools/ping
              methods:
                - GET
                - POST
              auth_required: true
              description: Network diagnostic helper.
              vulnerability_refs:
                - webapp-cmdi-ping
            - route_id: search
              path: /search
              methods:
                - GET
              auth_required: true
              description: Search users by name, email, or department.
              vulnerability_refs:
                - webapp-sqli-search
                - webapp-xss-reflected
            - route_id: comment
              path: /comment
              methods:
                - POST
              auth_required: true
              description: Post a comment.
              vulnerability_refs:
                - webapp-xss-stored
            - route_id: debug
              path: /debug
              methods:
                - GET
              auth_required: false
              description: Debug information endpoint.
              vulnerability_refs:
                - webapp-debug-endpoint
                - webapp-env-disclosure
            - route_id: robots
              path: /robots.txt
              methods:
                - GET
              auth_required: false
              description: Robots file.
  ad:
    type: vm
    os: linux
    source:
      name: "aptl-ad"
      version: local
      artifact_requirement:
        requirement_id: ad-image
        explicitness: constrained
        materialization_specifications:
          - specification_id: ad
            profile:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            digest: "sha256:8a891ee9c6483942c18116d135fba0e0aacfb4ec12da33cde6356ed32d0b5c46"
            locked_input_ids: []
        permitted_routes:
          - mechanism:
              mechanism: "materialization-specification"
              profile: "aptl-contained-component-build"
              version: "1"
              digest: "sha256:e728ca8aa2805d5fca152df145d4c331578caffec9b7dfe58d0c86a20b9e9c76"
            acquisition: none
            timing: backend-preparation
    services:
      - {name: ldap, port: 389, protocol: tcp}
      - {name: kerberos, port: 88, protocol: tcp}
      - {name: smb, port: 445, protocol: tcp}
  db:
    type: vm
    os: linux
    services:
      - {name: postgres, port: 5432, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /var/lib/postgresql/data, source: db_data, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: POSTGRES_DB
          value: "techvault"
          value_classification: plain
          provenance: compose
        - name: POSTGRES_USER
          value: "techvault"
          value_classification: plain
          provenance: compose
        - name: POSTGRES_PASSWORD
          value: "techvault_db_pass"
          value_classification: secret_fixture
          provenance: compose
      packages:
        - {manager: apt, name: postgresql, version: "*"}
      service_manager_units:
        - {unit_id: postgresql, unit_name: postgresql.service, enabled_state: enabled, active_state: active}
        - {unit_id: db-bootstrap, unit_name: db-bootstrap.service, enabled_state: enabled, active_state: active}
      database_services:
        - database_service_id: techvault-db
          engine: postgresql
          protocol: postgresql
          service: postgres
          description: The customer database. Its tables are what the web application exposes and what the SQL-injection path reaches.
          listeners:
            - address: 0.0.0.0
              port: 5432
          databases:
            - database_id: techvault
              name: techvault
              origin: scenario
              schemas:
                - schema_id: public
                  name: public
                  origin: scenario
                  tables:
                    - table_id: users
                      name: users
                    - table_id: customers
                      name: customers
                    - table_id: files
                      name: files
                    - table_id: api_keys
                      name: api_keys
                    - table_id: audit_log
                      name: audit_log
                    - table_id: backup_config
                      name: backup_config
                    - table_id: sessions
                      name: sessions
                    - table_id: comments
                      name: comments
          roles:
            - role_id: techvault
              name: techvault
              role_type: application
              origin: scenario
              can_login: true
              description: Application login the web tier authenticates as.
  workstation:
    type: vm
    os: linux
    services:
      - {name: ssh, port: 22, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /var/log, source: workstation_logs, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: SIEM_IP
          value: "172.20.2.30"
          value_classification: plain
          provenance: compose
        - name: LABADMIN_SSH_KEY_FILE
          value: "/keys/aptl_lab_key.pub"
          value_classification: plain
          provenance: compose
      packages:
        - {manager: dnf, name: openssh-server, version: "*"}
        # openssh-clients (the `ssh` binary) + postgresql (the `psql` client):
        # #581 parity for the compromised-workstation attack paths. dev-user's
        # leaked SSH key pivots into victim (workstation -> victim lateral
        # movement) and the leaked .pgpass reaches the DB; neither works without
        # a client. The v5.0.0 workstation image shipped both.
        - {manager: dnf, name: openssh-clients, version: "*"}
        - {manager: dnf, name: postgresql, version: "*"}
        - {manager: dnf, name: sudo, version: "*"}
      local_identity:
        groups:
          - {name: labadmin}
          - {name: dev-user}
        users:
          - {username: labadmin, primary_group: labadmin, supplemental_groups: [wheel]}
          # dev-user (michael.thompson): the compromised-workstation persona,
          # #581 parity. Distinct weak-cred developer account whose home holds
          # the planted credential loot the red-team exercise mines; separate
          # from the labadmin admin account. Loot placed via content below.
          - {username: dev-user, primary_group: dev-user, supplemental_groups: [wheel]}
      service_manager_units:
        - {unit_id: sshd, unit_name: sshd.service, enabled_state: enabled, active_state: active}
        # Per-boot CTF flags (#581 parity; see aptl-flaggen.sh content below).
        - {unit_id: aptl-flaggen, unit_name: aptl-flaggen.service, enabled_state: enabled, active_state: active}
        # Fix dev-user loot ownership + secret-file perms after the content copy
        # (the directory copy lands as labadmin:labadmin 664; .pgpass at 664 is
        # ignored by psql, and dev-user's home should be dev-user-owned).
        - {unit_id: aptl-workstation-loot, unit_name: aptl-workstation-loot.service, enabled_state: enabled, active_state: active}
  fileshare:
    type: vm
    os: linux
    services:
      - {name: smb, port: 445, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /srv/shares, source: fileshare_data, source_kind: volume}
        - {target: /var/log/samba, source: fileshare_logs, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: SIEM_IP
          value: "172.20.0.10"
          value_classification: plain
          provenance: compose
        - name: WAZUH_MANAGER
          value: "wazuh.manager"
          value_classification: plain
          provenance: compose
        - name: AGENT_NAME
          value: "aptl-fileshare-agent"
          value_classification: plain
          provenance: compose
        - name: LOG_PATHS
          value: "/var/log/samba/log.samba-bgqd,/var/log/samba/log.*"
          value_classification: plain
          provenance: compose
        - name: LOG_FORMAT
          value: "syslog"
          value_classification: plain
          provenance: compose
      packages:
        # continuity-audit execs iptables here to revert active-response
        # DROPs; the node declares CAP_NET_ADMIN for it, so the tool must
        # be present or the audit cannot inspect or clear the chain.
        - {manager: apt, name: iptables, version: "*"}
        - {manager: apt, name: samba, version: "*"}
      # aptl lab continuity-audit needs CAP_NET_ADMIN to revert blanket kali
      # source-IP DROPs on this node's INPUT chain (src/aptl/core/continuity.py).
      linux_capabilities:
        add: [CAP_NET_ADMIN]
      service_manager_units:
        - {unit_id: smbd, unit_name: smbd.service, enabled_state: enabled, active_state: active}
        # Per-boot CTF flags (#581 parity; see aptl-flaggen.sh content below).
        - {unit_id: aptl-flaggen, unit_name: aptl-flaggen.service, enabled_state: enabled, active_state: active}
      file_services:
        - file_service_id: techvault-fileshare
          protocol: smb
          service: smb
          description: Departmental SMB shares. Guest-readable shares and group-gated ones sit side by side, which is what makes lateral discovery meaningful here.
          shares:
            - share_id: public
              name: Public
              kind: disk
              backing_path: /srv/shares/public
              read_only: false
              browseable: true
              guest_ok: true
            - share_id: engineering
              name: Engineering
              kind: disk
              backing_path: /srv/shares/engineering
              read_only: false
              browseable: true
              guest_ok: false
              valid_groups:
                - Engineering
            - share_id: finance
              name: Finance
              kind: disk
              backing_path: /srv/shares/finance
              read_only: false
              browseable: true
              guest_ok: false
              valid_groups:
                - Finance
            - share_id: hr
              name: HR
              kind: disk
              backing_path: /srv/shares/hr
              read_only: false
              browseable: true
              guest_ok: false
              valid_groups:
                - HR
            - share_id: it-backups
              name: IT-Backups
              kind: disk
              backing_path: /srv/shares/it-backups
              read_only: false
              browseable: false
              guest_ok: false
              valid_groups:
                - IT-Admins
            - share_id: shared
              name: Shared
              kind: disk
              backing_path: /srv/shares/shared
              read_only: false
              browseable: true
              guest_ok: true
          principals:
            - principal_id: group-engineering
              kind: group
              name: Engineering
              origin: provisioned
            - principal_id: group-finance
              kind: group
              name: Finance
              origin: provisioned
            - principal_id: group-hr
              kind: group
              name: HR
              origin: provisioned
            - principal_id: group-it-admins
              kind: group
              name: IT-Admins
              origin: provisioned
  dns:
    type: vm
    os: linux
    services:
      - {name: dns, port: 53, protocol: udp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /var/log/named, source: dns_logs, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: WAZUH_MANAGER
          value: "wazuh.manager"
          value_classification: plain
          provenance: compose
        - name: AGENT_NAME
          value: "aptl-dns-agent"
          value_classification: plain
          provenance: compose
        - name: LOG_PATHS
          value: "/var/log/named/query.log,/var/log/named/default.log"
          value_classification: plain
          provenance: compose
        - name: LOG_FORMAT
          value: "syslog"
          value_classification: plain
          provenance: compose
      packages:
        # continuity-audit execs iptables here to revert active-response
        # DROPs; the node declares CAP_NET_ADMIN for it, so the tool must
        # be present or the audit cannot inspect or clear the chain.
        - {manager: apt, name: iptables, version: "*"}
        - {manager: apt, name: bind9, version: "*"}
        - {manager: apt, name: dnsutils, version: "*"}
      filesystem_inventory:
        - {path: /var/log/named, entry_type: directory, owner_user: bind, owner_group: bind}
      # aptl lab continuity-audit needs CAP_NET_ADMIN to revert blanket kali
      # source-IP DROPs on this node's INPUT chain (src/aptl/core/continuity.py).
      linux_capabilities:
        add: [CAP_NET_ADMIN]
      # Host port 5353 (53 is commonly already bound on the host by a local
      # resolver): both protocols, since DNS genuinely uses UDP for queries
      # and TCP for zone transfers / large responses. dns is a deliberate
      # attack-surface target like webapp (test_docker_compose_port_bindings.py
      # TARGET_SURFACES), so host_ip is explicit all-interfaces, not the
      # ADR-034 loopback default meant for operator/management surfaces
      # (issue #581). dns also sits on aptl-security (not internal: true),
      # so unlike webapp this publishes correctly without a proxy.
      network:
        published_ports:
          - {container_port: 53, protocol: tcp, host_port: 5353, host_ip: "0.0.0.0"}
          - {container_port: 53, protocol: udp, host_port: 5353, host_ip: "0.0.0.0"}
      service_manager_units:
        - {unit_id: named, unit_name: named.service, enabled_state: enabled, active_state: active}
      dns_services:
        - dns_service_id: techvault-dns
          implementation: bind
          service: dns
          description: Authoritative DNS for the range, forward and reverse.
          roles:
            - authoritative
          zones:
            - zone_id: techvault-local
              name: techvault.local
              kind: primary
              purpose: forward
              provenance: zone_file
              description: Forward zone naming the enterprise hosts.
            - zone_id: reverse-172-20
              name: 20.172.in-addr.arpa
              kind: primary
              purpose: reverse
              provenance: zone_file
              description: Reverse zone for the range address space.
  victim:
    type: vm
    os: linux
    services:
      - {name: ssh, port: 22, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /var/log, source: victim_logs, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: SIEM_IP
          value: "172.20.2.30"
          value_classification: plain
          provenance: compose
        - name: LABADMIN_SSH_KEY_FILE
          value: "/keys/aptl_lab_key.pub"
          value_classification: plain
          provenance: compose
      packages:
        - {manager: dnf, name: openssh-server, version: "*"}
        - {manager: dnf, name: sudo, version: "*"}
        # rsyslog forwards the victim's syslog to the Wazuh manager
        # (172.20.2.30:514) so host events (auth failures, the smoke
        # log-ingestion probe) reach the SIEM. #581 parity: the v5.0.0 victim
        # ran an in-process Wazuh agent; the generic node forwards via syslog.
        - {manager: dnf, name: rsyslog, version: "*"}
      local_identity:
        groups:
          - {name: labadmin}
        users:
          - {username: labadmin, primary_group: labadmin, supplemental_groups: [wheel]}
      service_manager_units:
        - {unit_id: sshd, unit_name: sshd.service, enabled_state: enabled, active_state: active}
        - {unit_id: aptl-rsyslog, unit_name: aptl-rsyslog.service, enabled_state: enabled, active_state: active}
        # Per-boot CTF flags (#581 parity; see aptl-flaggen.sh content below).
        - {unit_id: aptl-flaggen, unit_name: aptl-flaggen.service, enabled_state: enabled, active_state: active}

  kali:
    type: vm
    os: linux
    services:
      - {name: ssh, port: 22, protocol: tcp}
    runtime:
      # Named volumes the pre-refactor component owned. Bind mounts of
      # project keys and certificates are not restated here: they are
      # already authored as content placements, which carry containment
      # and sensitivity handling a raw bind does not.
      mounts:
        - {target: /home/kali/operations, source: kali_operations, source_kind: volume}
      # Environment the pre-refactor component required. Names and
      # classification only: a secret value is supplied by the operator
      # environment through the existing secret boundary, never authored
      # here and never carried in realization DTOs, argv, or logs.
      environment:
        - name: VICTIM_IP
          value: "172.20.2.20"
          value_classification: plain
          provenance: compose
      packages:
        - {manager: apt, name: openssh-server, version: "*"}
        # iputils-ping: the live gate's kali_reachability check ICMP-pings
        # every shared-network host from kali. nmap: telemetry_evidence_path
        # drives an nmap scan + failed-ssh-auth as the representative event
        # Suricata/Wazuh must correlate (issue #581).
        - {manager: apt, name: iputils-ping, version: "*"}
        - {manager: apt, name: nmap, version: "*"}
        # OBS-003 (ADR-033/ADR-041) per-session capture wiring: bsdmainutils
        # provides script(1) (the PTY recording pipe), python3 runs
        # aptl-capture-client, libcap2-bin provides capsh (drops
        # CAP_AUDIT_CONTROL from sshd so the kali user's passwordless sudo
        # cannot touch the audit trail the aptl-kali-capture sidecar owns).
        - {manager: apt, name: bsdmainutils, version: "*"}
        - {manager: apt, name: python3, version: "*"}
        - {manager: apt, name: libcap2-bin, version: "*"}
        # Offensive toolset — #581 parity with the v5.0.0 Kali image. The
        # live tests drive curl-based web attacks (SQLi/XSS/cmd-injection) and
        # the TechVault attack path uses these enumeration/exploitation tools.
        # NOTE: Kali metapackages (kali-linux-core / kali-tools-top10, which
        # carry metasploit) live in the Kali apt repo, not Debian, so restoring
        # those on the generic Debian base needs an upstream third-party-repo
        # materializer capability (aces#847). Here we install the
        # Debian-available subset the scenario actually exercises.
        - {manager: apt, name: curl, version: "*"}
        - {manager: apt, name: wget, version: "*"}
        - {manager: apt, name: dnsutils, version: "*"}
        - {manager: apt, name: netcat-openbsd, version: "*"}
        - {manager: apt, name: smbclient, version: "*"}
        - {manager: apt, name: ldap-utils, version: "*"}
        - {manager: apt, name: sqlmap, version: "*"}
        - {manager: apt, name: hydra, version: "*"}
        - {manager: apt, name: python3-impacket, version: "*"}
      local_identity:
        groups:
          - {name: kali}
        users:
          - {username: kali, primary_group: kali}
      service_manager_units:
        - {unit_id: ssh, unit_name: ssh.service, enabled_state: enabled, active_state: active}
        - {unit_id: kali-capture-bootstrap, unit_name: kali-capture-bootstrap.service, enabled_state: enabled, active_state: active}
  kali-capture:
    type: vm
    os: linux
    services: []
  kali-ssh-proxy:
    type: vm
    os: linux
    description: >-
      Operator SSH entry point into the red-team network. Bridges the control
      network to Kali's SSH service so the range's SSH port is reachable without
      attaching the operator's host to the red-team network directly.
    services:
      - {name: ssh, port: 2023, protocol: tcp}
  webapp-proxy:
    type: vm
    os: linux
    description: >-
      Operator HTTP entry point to the customer portal. Bridges the control
      network to the webapp's service port on the DMZ so the portal is reachable
      from the host while the DMZ itself stays internal.
    services:
      - {name: http, port: 8080, protocol: tcp}
  cortex-index-init:
    type: vm
    os: linux
    description: >-
      One-shot initializer that creates Cortex's Elasticsearch index against
      thehive-es before Cortex starts, then exits. It is a short-lived member of
      the range, not a long-running host, and is declared because it starts.
    services: []

  aptl-otel-collector:
    type: vm
    os: linux
    source:
      name: "otel/opentelemetry-collector-contrib"
      version: "sha256:e39311df1f3d941923c00da79ac7ba6269124a870ee87e3c3ad24d60f8aee4d2"
      artifact_requirement:
        requirement_id: aptl-otel-collector-image
        explicitness: exact
        exact_artifact:
          artifact_id: "otel/opentelemetry-collector-contrib"
          version: "sha256:e39311df1f3d941923c00da79ac7ba6269124a870ee87e3c3ad24d60f8aee4d2"
          digest: "sha256:e39311df1f3d941923c00da79ac7ba6269124a870ee87e3c3ad24d60f8aee4d2"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: otlp-grpc, port: 4317, protocol: tcp}
      - {name: otlp-http, port: 4318, protocol: tcp}
  aptl-tempo:
    type: vm
    os: linux
    source:
      name: "grafana/tempo"
      version: "sha256:4b0277a9b572a4b1fa43b01468e58911ec214dd69d033f2cd476596d14de9fe3"
      artifact_requirement:
        requirement_id: aptl-tempo-image
        explicitness: exact
        exact_artifact:
          artifact_id: "grafana/tempo"
          version: "sha256:4b0277a9b572a4b1fa43b01468e58911ec214dd69d033f2cd476596d14de9fe3"
          digest: "sha256:4b0277a9b572a4b1fa43b01468e58911ec214dd69d033f2cd476596d14de9fe3"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: tempo-http, port: 3200, protocol: tcp}
  aptl-grafana-otel:
    type: vm
    os: linux
    source:
      name: "grafana/grafana"
      version: "sha256:62d2b9d20a19714ebfe48d1bb405086081bc602aa053e28cf6d73c7537640dfb"
      artifact_requirement:
        requirement_id: aptl-grafana-otel-image
        explicitness: exact
        exact_artifact:
          artifact_id: "grafana/grafana"
          version: "sha256:62d2b9d20a19714ebfe48d1bb405086081bc602aa053e28cf6d73c7537640dfb"
          digest: "sha256:62d2b9d20a19714ebfe48d1bb405086081bc602aa053e28cf6d73c7537640dfb"
          media_type: "application/vnd.docker.distribution.manifest.list.v2+json"
        permitted_routes:
          - mechanism:
              mechanism: "exact-artifact"
              profile: "aptl-oci-exact-pull"
              version: "1"
              digest: "sha256:041966e3e607d90c1237bdbbf1a7a8b48de397ea134405094318f41521b8dfea"
            acquisition: pull
            timing: backend-preparation
    services:
      - {name: grafana, port: 3000, protocol: tcp}

infrastructure:
  security-net:
    properties: {cidr: 172.20.0.0/24, gateway: 172.20.0.1, internal: false}
  dmz-net:
    properties: {cidr: 172.20.1.0/24, gateway: 172.20.1.1, internal: true}
  internal-net:
    properties: {cidr: 172.20.2.0/24, gateway: 172.20.2.1, internal: true}
  redteam-net:
    properties: {cidr: 172.20.4.0/24, gateway: 172.20.4.1, internal: true}
  control-net:
    properties: {cidr: 172.20.5.0/24, gateway: 172.20.5.1, internal: false}

  wazuh-manager:
    links: [security-net, dmz-net, internal-net]
    dependencies: [wazuh-indexer]
    properties:
      - {security-net: 172.20.0.10}
      - {dmz-net: 172.20.1.10}
      - {internal-net: 172.20.2.30}
  wazuh-indexer:
    links: [security-net]
    properties:
      - {security-net: 172.20.0.12}
  wazuh-dashboard:
    links: [security-net]
    dependencies: [wazuh-indexer, wazuh-manager]

  suricata:
    links: [security-net, dmz-net, internal-net]
  misp:
    links: [security-net]
    dependencies: [misp-db, misp-redis]
  misp-db:
    links: [security-net]
  misp-redis:
    links: [security-net]
  misp-suricata-sync:
    links: [security-net]
    dependencies: [misp, suricata]
  thehive:
    links: [security-net]
    dependencies: [thehive-cassandra, thehive-es, cortex]
  thehive-cassandra:
    links: [security-net]
  thehive-es:
    links: [security-net]
  cortex:
    links: [security-net]
    dependencies: [thehive-es]
  shuffle-backend:
    links: [security-net]
    dependencies: [shuffle-opensearch]
  shuffle-frontend:
    links: [security-net]
    dependencies: [shuffle-backend]
  shuffle-orborus:
    links: [security-net]
    dependencies: [shuffle-backend]
  shuffle-opensearch:
    links: [security-net]
  wazuh-sidecar-db:
    links: [security-net]
    dependencies: [wazuh-manager, db]
  wazuh-sidecar-suricata:
    links: [security-net]
    dependencies: [wazuh-manager, suricata]

  # Fixed addressing for the enterprise + attacker set. #581 parity: the
  # v5.0.0 fixed-compose lab assigned these deterministically, and the DNS
  # zones, seeded credentials/loot, and the live tests all hardcode them.
  # Without a pinned address the node is auto-allocated and drifts.
  webapp:
    links: [dmz-net, internal-net]
    dependencies: [db, wazuh-manager]
    properties:
      - {dmz-net: 172.20.1.20}
      - {internal-net: 172.20.2.25}
  ad:
    links: [internal-net]
    dependencies: [wazuh-manager]
    properties:
      - {internal-net: 172.20.2.10}
  db:
    links: [internal-net]
    properties:
      - {internal-net: 172.20.2.11}
  workstation:
    links: [internal-net]
    dependencies: [wazuh-manager]
    properties:
      - {internal-net: 172.20.2.40}
  fileshare:
    links: [internal-net]
    dependencies: [wazuh-manager]
    properties:
      - {internal-net: 172.20.2.12}
  dns:
    links: [security-net, dmz-net, internal-net]
    dependencies: [wazuh-manager]
    properties:
      - {security-net: 172.20.0.25}
      - {dmz-net: 172.20.1.22}
      - {internal-net: 172.20.2.27}
  victim:
    links: [internal-net]
    dependencies: [wazuh-manager]
    properties:
      - {internal-net: 172.20.2.20}

  kali:
    links: [redteam-net, dmz-net, internal-net]
    properties:
      - {redteam-net: 172.20.4.30}
      - {dmz-net: 172.20.1.30}
      - {internal-net: 172.20.2.35}
  kali-capture:
    dependencies: [kali]

  aptl-otel-collector:
    links: [security-net]
  aptl-tempo:
    links: [security-net]
  aptl-grafana-otel:
    links: [security-net]
    dependencies: [aptl-tempo]

# Stateful Wazuh prerequisites are authored RAES resources. The deployment
# backend materializes and verifies these before either consumer starts; the
# checked-in Compose service definitions are provider bindings, not the source
# of artifact, volume, or dependency intent.
generated_artifacts:
  wazuh-indexer-certs:
    generator: certificate_bundle
    lifecycle: reuse_valid
    provenance: config/certs.yml
    outputs:
      - {name: root-ca, path: root-ca.pem, sensitivity: public}
      - {name: indexer-key, path: wazuh.indexer-key.pem, sensitivity: secret}
      - {name: indexer-cert, path: wazuh.indexer.pem, sensitivity: public}
    consumers:
      - node: wazuh-indexer
        mount_destination: /usr/share/wazuh-indexer/certs
        access_mode: read_only
  wazuh-manager-certs:
    generator: certificate_bundle
    lifecycle: reuse_valid
    provenance: config/certs.yml
    outputs:
      - {name: manager-root-ca, path: root-ca-manager.pem, sensitivity: public}
      - {name: manager-key, path: wazuh.manager-key.pem, sensitivity: secret}
      - {name: manager-cert, path: wazuh.manager.pem, sensitivity: public}
    consumers:
      - node: wazuh-manager
        mount_destination: /etc/ssl/wazuh
        access_mode: read_only
  wazuh-dashboard-certs:
    generator: certificate_bundle
    lifecycle: reuse_valid
    provenance: config/certs.yml
    outputs:
      - {name: root-ca, path: root-ca.pem, sensitivity: public}
      - {name: dashboard-key, path: wazuh.dashboard-key.pem, sensitivity: secret}
      - {name: dashboard-cert, path: wazuh.dashboard.pem, sensitivity: public}
    consumers:
      - node: wazuh-dashboard
        mount_destination: /usr/share/wazuh-dashboard/certs
        access_mode: read_only
  wazuh-manager-config:
    generator: rendered_config
    lifecycle: regenerate_on_change
    provenance: config/wazuh_cluster/wazuh_manager.conf
    outputs:
      - {name: manager-config, path: wazuh_manager.conf, sensitivity: secret}
    consumers:
      - node: wazuh-manager
        mount_destination: /wazuh-config-mount/etc/ossec.conf
        access_mode: read_only
    ordering_dependencies: [generated_artifacts.wazuh-manager-certs]

persistent_volumes:
  wazuh-indexer-data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - node: wazuh-indexer
        mount_destination: /var/lib/wazuh-indexer
        access_mode: read_write
    ordering_dependencies: [generated_artifacts.wazuh-indexer-certs]
  wazuh-manager-etc:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - node: wazuh-manager
        mount_destination: /var/ossec/etc
        access_mode: read_write
    ordering_dependencies: [generated_artifacts.wazuh-manager-config]
  wazuh-manager-logs:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - node: wazuh-manager
        mount_destination: /var/ossec/logs
        access_mode: read_write
    ordering_dependencies: [persistent_volumes.wazuh-manager-etc]

  ad_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: ad, mount_destination: /var/lib/samba, access_mode: read_write}
  ad_logs:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: ad, mount_destination: /var/log/samba, access_mode: read_write}
  cortex_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: cortex, mount_destination: /opt/cortex/jobs, access_mode: read_write}
  grafana_otel_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: aptl-grafana-otel, mount_destination: /var/lib/grafana, access_mode: read_write}
  kali_captures:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: kali-capture, mount_destination: /var/log/aptl/captures, access_mode: read_write}
  misp_config:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: misp, mount_destination: /var/www/MISP/app/Config, access_mode: read_write}
  misp_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: misp, mount_destination: /var/www/MISP/app/files, access_mode: read_write}
  misp_db_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: misp-db, mount_destination: /var/lib/mysql, access_mode: read_write}
  shuffle_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: shuffle-backend, mount_destination: /shuffle-database, access_mode: read_write}
  shuffle_opensearch_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: shuffle-opensearch, mount_destination: /usr/share/opensearch/data, access_mode: read_write}
  suricata_config_seed:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: suricata, mount_destination: /seed, access_mode: read_only}
  suricata_logs:
    lifecycle: retain
    access_mode: read_write_once
      # Suricata writes eve.json here and the Wazuh sidecar tails it read-only:
      # this volume is the telemetry hand-off between detection and shipping.
    consumers:
      - {node: suricata, mount_destination: /var/log/suricata, access_mode: read_write}
      - {node: wazuh-sidecar-suricata, mount_destination: /logs, access_mode: read_only}
  tempo_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: aptl-tempo, mount_destination: /var/tempo, access_mode: read_write}
  thehive_cassandra_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: thehive-cassandra, mount_destination: /var/lib/cassandra, access_mode: read_write}
  thehive_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: thehive, mount_destination: /opt/thp/thehive/data, access_mode: read_write}
  thehive_es_data:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: thehive-es, mount_destination: /usr/share/elasticsearch/data, access_mode: read_write}
  thehive_index:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: thehive, mount_destination: /opt/thp/thehive/index, access_mode: read_write}
  wazuh-dashboard-config:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: wazuh-dashboard, mount_destination: /usr/share/wazuh-dashboard/data/wazuh/config, access_mode: read_write}
  wazuh-dashboard-custom:
    lifecycle: retain
    access_mode: read_write_once
    consumers:
      - {node: wazuh-dashboard, mount_destination: /usr/share/wazuh-dashboard/plugins/wazuh/public/assets/custom, access_mode: read_write}
features:
  techvault-webapp-service:
    type: service
    source:
      name: aptl-webapp
      version: local
    description: TechVault vulnerable customer portal service.
  techvault-defensive-stack:
    type: service
    source:
      name: aptl-soc-stack
      version: local
    description: Wazuh, Suricata, MISP, TheHive, Cortex, and Shuffle services.

vulnerabilities:
  webapp-sqli-login:
    name: SQL injection in login
    description: Login form accepts intentionally vulnerable SQL input.
    technical: true
    class: CWE-89

# Realizable content (ADR-046 TechVault Operational Standup Addendum,
# issue #689): every entry here is either bounded inline text or a
# project-contained, checked-in source. No runtime-observed:, dataset, or
# captured-filesystem content is authored here — APTL does not carry a
# separate captured inventory as review evidence (ADR-046 Capture Inventory
# and Parity-Inventory Removal Addendum). Both entries land on the
# `fileshare` node, whose Compose service mounts the `fileshare_data`
# named volume the APTL content realization backend seeds at
# `aptl lab start`.
  webapp-sqli-search:
    name: SQL injection in search
    description: The /search query parameter is interpolated into SQL. This is the path the range's detection chain exercises.
    technical: true
    class: CWE-89
  webapp-cmdi-ping:
    name: Command injection in the ping tool
    description: The /tools/ping host parameter reaches a shell without sanitization.
    technical: true
    class: CWE-78
  webapp-xss-reflected:
    name: Reflected XSS in search results
    description: The search query is rendered back into the page unescaped.
    technical: true
    class: CWE-79
  webapp-xss-stored:
    name: Stored XSS in comments
    description: Comment bodies are stored and rendered without sanitization.
    technical: true
    class: CWE-79
  webapp-idor-files:
    name: IDOR on the file API
    description: /api/v1/files/<id> performs no ownership check.
    technical: true
    class: CWE-639
  webapp-idor-users:
    name: IDOR on the user API
    description: /api/v1/users/<id> performs no authorization check and returns an API key.
    technical: true
    class: CWE-639
  webapp-missing-authz-admin:
    name: Missing role check on the admin page
    description: /admin is reachable by any authenticated user and exposes stored cloud credentials.
    technical: true
    class: CWE-862
  webapp-weak-jwt:
    name: Weak JWT secret with no expiry
    description: Tokens are signed with a guessable secret and never expire.
    technical: true
    class: CWE-330
  webapp-hardcoded-secrets:
    name: Hardcoded application secrets
    description: The Flask session key and JWT secret are literals in the application source.
    technical: true
    class: CWE-798
  webapp-unrestricted-upload:
    name: Unrestricted file upload
    description: Uploads are neither type- nor size-checked, and the filename allows traversal.
    technical: true
    class: CWE-434
  webapp-debug-endpoint:
    name: Debug endpoint exposed
    description: /debug is reachable in the running application.
    technical: true
    class: CWE-489
  webapp-env-disclosure:
    name: Environment file disclosure
    description: The application serves its own .env content.
    technical: true
    class: CWE-538
  webapp-verbose-errors:
    name: Verbose error disclosure
    description: Error responses reveal internal detail.
    technical: true
    class: CWE-209
content:
  misp-suricata-sync-pyproject:
    type: file
    target: misp-suricata-sync
    path: /app/pyproject.toml
    source:
      name: pyproject.toml
  misp-suricata-sync-readme:
    type: file
    target: misp-suricata-sync
    path: /app/README.md
    source:
      name: README.md
  misp-suricata-sync-hatch-build:
    type: file
    target: misp-suricata-sync
    path: /app/hatch_build.py
    source:
      name: hatch_build.py
  misp-suricata-sync-src:
    type: directory
    target: misp-suricata-sync
    destination: /app/src
    source:
      name: src
  webapp-app-code:
    type: directory
    target: webapp
    destination: /app
    source:
      name: containers/webapp/app
  webapp-service-unit:
    type: file
    target: webapp
    path: /etc/systemd/system/webapp.service
    text: |
      [Unit]
      Description=TechVault webapp (gunicorn)
      After=network.target

      [Service]
      WorkingDirectory=/app
      ExecStart=/usr/bin/python3 -m gunicorn --bind 0.0.0.0:8080 --workers 2 --access-logfile /var/log/gunicorn/access.log --error-logfile - app:app
      Restart=always

      [Install]
      WantedBy=multi-user.target
  # rsyslog forward configs (#581 Tier-2 monitoring parity). imfile reads
  # gunicorn's access log and forwards every message to the Wazuh manager's
  # remote-syslog port; the gunicorn-tagged access lines are what the
  # web-accesslog decoder + rules 302010/302020/302030 match (verified live:
  # a forwarded SQLi line fires 302010). victim forwards its host syslog so
  # auth failures + the smoke log-ingestion probe reach the SIEM.
  webapp-rsyslog-forward:
    type: file
    target: webapp
    path: /etc/rsyslog.d/90-aptl-forward.conf
    text: |
      module(load="imfile")
      # Forward gunicorn access lines RAW (no syslog program tag) so the
      # manager's built-in web-accesslog decoder matches them and rules
      # 302010/302020/302030 fire. A program tag (e.g. "gunicorn:") makes the
      # manager's predecoder claim the line as that program and the
      # web-accesslog decoder then no longer matches (verified live).
      template(name="aptl_rawfwd" type="string" string="<134>%timestamp:::date-rfc3164% %hostname% %msg%\n")
      input(type="imfile"
            File="/var/log/gunicorn/access.log"
            Tag="webapp"
            ruleset="aptl_fwd")
      ruleset(name="aptl_fwd") {
        action(type="omfwd" Target="172.20.2.30" Port="514" Protocol="udp" Template="aptl_rawfwd")
      }
  victim-rsyslog-forward:
    type: file
    target: victim
    path: /etc/rsyslog.d/90-aptl-forward.conf
    text: |
      *.* @172.20.2.30:514
  # Custom rsyslog units for the generic systemd nodes (see the
  # service_manager_units note on webapp): a fresh unit with no sandboxing so
  # systemd can start rsyslogd under the materializer's restricted caps.
  webapp-rsyslog-unit:
    type: file
    target: webapp
    path: /etc/systemd/system/aptl-rsyslog.service
    text: |
      [Unit]
      Description=APTL rsyslog log forwarder
      After=network.target
      [Service]
      ExecStart=/usr/sbin/rsyslogd -n
      Restart=always
      [Install]
      WantedBy=multi-user.target
  victim-rsyslog-unit:
    type: file
    target: victim
    path: /etc/systemd/system/aptl-rsyslog.service
    text: |
      [Unit]
      Description=APTL rsyslog log forwarder
      After=network.target
      [Service]
      ExecStart=/usr/sbin/rsyslogd -n
      Restart=always
      [Install]
      WantedBy=multi-user.target
  dns-named-conf:
    type: file
    target: dns
    path: /etc/bind/named.conf
    source:
      name: containers/dns/named.conf
  dns-zone-fwd:
    type: file
    target: dns
    path: /etc/bind/zones/techvault.local.zone
    source:
      name: containers/dns/zones/techvault.local.zone
  dns-zone-rev:
    type: file
    target: dns
    path: /etc/bind/zones/172.20.rev
    source:
      name: containers/dns/zones/172.20.rev
  fileshare-smb-conf:
    type: file
    target: fileshare
    path: /etc/samba/smb.conf
    source:
      name: containers/fileshare/smb.conf
  # Full TechVault file-share loot tree — #581 parity. smb.conf advertises
  # Public/Engineering/Finance/HR/IT-Backups/Shared; the red-team exercise
  # mines the planted creds (engineering/deployments/deploy.sh), PII
  # (hr/employees/directory.csv), wifi passwords (shared/wifi-passwords.txt),
  # and a leaked deploy key (it-backups/keys/). Realized from a checked-in
  # fixture, not baked onto an image. Keeps the onboarding + public notice.
  fileshare-shares:
    type: directory
    target: fileshare
    destination: /srv/shares
    source:
      name: scenarios/fixtures/techvault-content/fileshare-shares
  # Workstation dev-user loot home — #581 parity (WS-01..WS-05). Planted
  # credential artifacts (.bash_history, .pgpass, .config/credentials.json,
  # projects/techvault-portal/{.env,deploy.sh}, Documents/onboarding-notes.txt,
  # .ssh keypair) the attack path harvests off the compromised workstation.
  workstation-dev-user-home:
    type: directory
    target: workstation
    destination: /home/dev-user
    source:
      name: scenarios/fixtures/techvault-content/dev-user-home
  # dev-user's leaked SSH private key (loot WS-03) is the real workstation->
  # victim lateral-movement pivot: the standup-generated workstation pivot
  # keypair (config/lab-ssh/workstation_pivot_key, ensure_workstation_pivot_key
  # in src/aptl/core/ssh.py) whose public half victim already authorizes
  # (victim-authorized-keys below). Placing its private half here as dev-user's
  # ~/.ssh/id_rsa reproduces the v5.0.0 workstation entrypoint's throwaway
  # dev-user keypair, so `ssh -i ~/.ssh/id_rsa labadmin@victim` from the
  # compromised workstation works. The key is never tracked (lab-ssh is
  # gitignored + generated per standup), so no id_rsa/PEM secret ships in-repo.
  workstation-dev-user-privkey:
    type: file
    target: workstation
    path: /home/dev-user/.ssh/id_rsa
    source:
      name: config/lab-ssh/workstation_pivot_key
    sensitive: true
  # The matching public half, so dev-user's keypair is self-consistent. This
  # must be the pivot key's real .pub: `ssh -i id_rsa` reads id_rsa.pub (not the
  # private key) to pick which key to offer, so a mismatched .pub makes ssh
  # offer the wrong key and victim rejects the pivot. Sourced from the same
  # standup-generated keypair (not the fixture, which cannot know the per-lab
  # key) — the fixture's stale placeholder .pub was removed for this reason.
  workstation-dev-user-pubkey:
    type: file
    target: workstation
    path: /home/dev-user/.ssh/id_rsa.pub
    source:
      name: config/lab-ssh/workstation_pivot_key.pub
  # The stock `postgresql` package creates an empty cluster with no
  # techvault database/user and no schema — the legacy compose db service
  # got these from postgres:16-alpine's POSTGRES_DB/USER/PASSWORD
  # auto-init plus containers/db/init/*.sql mounted at
  # /docker-entrypoint-initdb.d, neither of which apply to a generically
  # materialized node. db-bootstrap.service (below) reproduces both by
  # hand: opens postgres to the network, creates the techvault role/
  # database, then loads the same schema/seed SQL webapp's own defaults
  # (containers/webapp/app/app.py) already expect (issue #581).
  db-init-schema:
    type: file
    target: db
    path: /opt/db-init/01-schema.sql
    source:
      name: containers/db/init/01-schema.sql
  db-init-seed:
    type: file
    target: db
    path: /opt/db-init/02-seed-data.sql
    source:
      name: containers/db/init/02-seed-data.sql
  db-bootstrap-script:
    type: file
    target: db
    path: /opt/db-bootstrap.sh
    text: |
      #!/bin/bash
      set -e
      PG_CONF=$(find /etc/postgresql -maxdepth 3 -name postgresql.conf | head -n1)
      PG_HBA=$(find /etc/postgresql -maxdepth 3 -name pg_hba.conf | head -n1)
      sed -i "s/^#\?listen_addresses.*/listen_addresses = '*'/" "$PG_CONF"
      grep -q "^host techvault techvault " "$PG_HBA" || echo "host techvault techvault 0.0.0.0/0 md5" >> "$PG_HBA"
      systemctl restart postgresql
      for i in $(seq 1 30); do
        su -s /bin/bash postgres -c "pg_isready -q" && break
        sleep 1
      done
      su -s /bin/bash postgres -c "psql -tAc \"SELECT 1 FROM pg_roles WHERE rolname='techvault'\"" | grep -q 1 || \
        su -s /bin/bash postgres -c "psql -c \"CREATE USER techvault WITH PASSWORD 'techvault_db_pass';\""
      su -s /bin/bash postgres -c "psql -tAc \"SELECT 1 FROM pg_database WHERE datname='techvault'\"" | grep -q 1 || \
        su -s /bin/bash postgres -c "psql -c \"CREATE DATABASE techvault OWNER techvault;\""
      PGPASSWORD=techvault_db_pass psql -h 127.0.0.1 -U techvault -d techvault -f /opt/db-init/01-schema.sql
      PGPASSWORD=techvault_db_pass psql -h 127.0.0.1 -U techvault -d techvault -f /opt/db-init/02-seed-data.sql
  db-bootstrap-unit:
    type: file
    target: db
    path: /etc/systemd/system/db-bootstrap.service
    text: |
      [Unit]
      Description=TechVault database bootstrap (create techvault DB/user, load schema+seed)
      After=postgresql.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/bin/bash /opt/db-bootstrap.sh

      [Install]
      WantedBy=multi-user.target
  # SEC #417: victim/workstation authorize both the operator control-plane
  # key and the kali pivot key (target_authorized_keys is the two combined,
  # generated at lab standup — src/aptl/core/ssh.py). kali itself is
  # authorized with only the control-plane key, and separately receives the
  # pivot key's *private* half to SSH out into these targets. This mirrors
  # the entrypoint-script SSH provisioning the legacy hand-authored images
  # ran, now realized generically from declared content (issue #581).
  #
  # victim additionally trusts the workstation pivot key below (Prime
  # scenario lateral movement, workstation -> victim), so it gets its own
  # combined file instead of sharing target_authorized_keys with workstation.
  victim-authorized-keys:
    type: file
    target: victim
    path: /home/labadmin/.ssh/authorized_keys
    source:
      name: keys/victim_authorized_keys
  workstation-authorized-keys:
    type: file
    target: workstation
    path: /home/labadmin/.ssh/authorized_keys
    source:
      name: keys/target_authorized_keys
  # workstation's private half of the lateral-movement pivot into victim;
  # placed as the default identity file so `ssh labadmin@victim` from
  # workstation needs no -i flag, matching how the legacy entrypoint's
  # throwaway dev-user keypair was used (issue #581).
  workstation-pivot-key:
    type: file
    target: workstation
    path: /home/labadmin/.ssh/id_ed25519
    source:
      name: config/lab-ssh/workstation_pivot_key
    sensitive: true
  kali-authorized-keys:
    type: file
    target: kali
    path: /home/kali/.ssh/authorized_keys
    source:
      name: keys/authorized_keys
  kali-pivot-key:
    type: file
    target: kali
    path: /home/kali/.ssh/kali_pivot_key
    source:
      name: config/lab-ssh/kali_pivot_key
    sensitive: true
  # OBS-003 (ADR-033/ADR-041) per-session capture wiring, restored after the
  # SDL conversion dropped it (issue #581): sshd's ForceCommand hands every
  # kali-user session to aptl-wrap-shell.sh, which records the PTY via
  # script(1) and streams it to the aptl-kali-capture sidecar (a separate,
  # still-Compose-managed node sharing kali's network namespace) instead of
  # writing evidence inside this container, where the kali user's
  # passwordless sudo could tamper with it.
  kali-wrap-shell-script:
    type: file
    target: kali
    path: /usr/local/bin/aptl-wrap-shell.sh
    source:
      name: containers/kali/scripts/aptl-wrap-shell.sh
  kali-capture-client:
    type: file
    target: kali
    path: /usr/local/bin/aptl-capture-client
    source:
      name: containers/kali/scripts/aptl-capture-client
  # Drops CAP_AUDIT_CONTROL from sshd itself — belt-and-suspenders on top of
  # kali's container capability set (which never includes AUDIT_CONTROL to
  # begin with; that capability lives only on the capture sidecar).
  kali-ssh-capsh-override:
    type: file
    target: kali
    path: /etc/systemd/system/ssh.service.d/override.conf
    text: |
      [Service]
      ExecStart=
      ExecStart=/usr/sbin/capsh --drop=cap_audit_control -- -c "exec /usr/sbin/sshd -D $SSHD_OPTS"
  # Appends the ForceCommand/AcceptEnv wiring to the stock sshd_config and
  # applies both this and the capsh override above with one restart —
  # neither content: placement above can itself trigger sshd to notice a
  # config change it may already have started with (issue #581, same
  # pattern as db-bootstrap.sh).
  kali-capture-bootstrap-script:
    type: file
    target: kali
    path: /opt/kali-capture-bootstrap.sh
    text: |
      #!/bin/bash
      set -e
      if ! grep -q "^AcceptEnv APTL_SESSION_ID" /etc/ssh/sshd_config; then
        {
          echo ""
          echo "# OBS-003 / ADR-033: per-session capture wiring"
          echo "AcceptEnv APTL_SESSION_ID APTL_RUN_ID APTL_TRACE_ID"
          echo "Match User kali"
          echo "    ForceCommand /bin/bash /usr/local/bin/aptl-wrap-shell.sh"
        } >> /etc/ssh/sshd_config
      fi
      sshd -t
      systemctl daemon-reload
      systemctl restart ssh
  kali-capture-bootstrap-unit:
    type: file
    target: kali
    path: /etc/systemd/system/kali-capture-bootstrap.service
    text: |
      [Unit]
      Description=TechVault kali OBS-003 per-session capture wiring
      After=ssh.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/bin/bash /opt/kali-capture-bootstrap.sh

      [Install]
      WantedBy=multi-user.target

  # ---------------------------------------------------------------------------
  # CTF flag generation (#581 parity). The v5.0.0 base image and the still-image
  # AD node generate per-boot signed flags at container start; the image-free
  # generic nodes (victim, workstation, webapp, fileshare) reproduce it with a
  # oneshot systemd unit running the shared aptl-flaggen.sh. flags.py (scoring)
  # reads /home/labadmin|dev-user/user.txt, /app/user.txt,
  # /srv/shares/shared/user-flag.txt, and /root/root.txt and verifies the
  # aptl:v1 md5 token. One script; each node's unit supplies its Environment=.
  # ---------------------------------------------------------------------------
  victim-flaggen-script:
    type: file
    target: victim
    path: /usr/local/sbin/aptl-flaggen.sh
    source:
      name: scenarios/fixtures/techvault-content/aptl-flaggen.sh
  workstation-flaggen-script:
    type: file
    target: workstation
    path: /usr/local/sbin/aptl-flaggen.sh
    source:
      name: scenarios/fixtures/techvault-content/aptl-flaggen.sh
  webapp-flaggen-script:
    type: file
    target: webapp
    path: /usr/local/sbin/aptl-flaggen.sh
    source:
      name: scenarios/fixtures/techvault-content/aptl-flaggen.sh
  fileshare-flaggen-script:
    type: file
    target: fileshare
    path: /usr/local/sbin/aptl-flaggen.sh
    source:
      name: scenarios/fixtures/techvault-content/aptl-flaggen.sh
  victim-flaggen-unit:
    type: file
    target: victim
    path: /etc/systemd/system/aptl-flaggen.service
    text: |
      [Unit]
      Description=APTL CTF flag generator
      After=local-fs.target

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      Environment=APTL_FLAG_NODE=victim
      Environment=APTL_FLAG_USER_PATH=/home/labadmin/user.txt
      Environment=APTL_FLAG_USER_OWNER=labadmin:labadmin
      ExecStart=/bin/bash /usr/local/sbin/aptl-flaggen.sh

      [Install]
      WantedBy=multi-user.target
  workstation-flaggen-unit:
    type: file
    target: workstation
    path: /etc/systemd/system/aptl-flaggen.service
    text: |
      [Unit]
      Description=APTL CTF flag generator
      After=local-fs.target

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      Environment=APTL_FLAG_NODE=workstation
      Environment=APTL_FLAG_USER_PATH=/home/dev-user/user.txt
      Environment=APTL_FLAG_USER_OWNER=dev-user:dev-user
      ExecStart=/bin/bash /usr/local/sbin/aptl-flaggen.sh

      [Install]
      WantedBy=multi-user.target
  webapp-flaggen-unit:
    type: file
    target: webapp
    path: /etc/systemd/system/aptl-flaggen.service
    text: |
      [Unit]
      Description=APTL CTF flag generator
      After=local-fs.target

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      Environment=APTL_FLAG_NODE=webapp
      Environment=APTL_FLAG_USER_PATH=/app/user.txt
      Environment=APTL_FLAG_USER_OWNER=root:root
      ExecStart=/bin/bash /usr/local/sbin/aptl-flaggen.sh

      [Install]
      WantedBy=multi-user.target
  fileshare-flaggen-unit:
    type: file
    target: fileshare
    path: /etc/systemd/system/aptl-flaggen.service
    text: |
      [Unit]
      Description=APTL CTF flag generator
      After=local-fs.target

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      Environment=APTL_FLAG_NODE=fileshare
      Environment=APTL_FLAG_USER_PATH=/srv/shares/shared/user-flag.txt
      Environment=APTL_FLAG_USER_OWNER=root:root
      ExecStart=/bin/bash /usr/local/sbin/aptl-flaggen.sh

      [Install]
      WantedBy=multi-user.target
  # Finalizes dev-user loot after the directory copy: the copy lands the home
  # as labadmin:labadmin 664, but psql ignores a group/world-readable .pgpass
  # and dev-user's home should be dev-user-owned. chown + tighten the secret
  # files to 600 (matching the v5.0.0 setup-workstation.sh perms). Best-effort
  # per file so a fixture reshuffle never fails the whole boot.
  workstation-loot-unit:
    type: file
    target: workstation
    path: /etc/systemd/system/aptl-workstation-loot.service
    text: |
      [Unit]
      Description=APTL workstation dev-user loot ownership + perms
      After=local-fs.target

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/bin/bash -c 'chown -R dev-user:dev-user /home/dev-user 2>/dev/null; for f in /home/dev-user/.pgpass /home/dev-user/.ssh/id_rsa /home/dev-user/.bash_history /home/dev-user/.config/credentials.json /home/dev-user/projects/techvault-portal/.env; do [ -f "$f" ] && chmod 600 "$f"; done; true'

      [Install]
      WantedBy=multi-user.target

# Realizable accounts (ADR-046 Account and Identity Realization Addendum,
# #577): the deployment backend's realize_accounts operation ensures these
# groups, users, memberships, and non-secret attributes on the resolved `ad`
# node and verifies them by read-after-write. Declares identity only (no
# password material); the concrete credential is generated inside the target
# provider and never crosses the SDL or run evidence. Accounts that also exist
# in containers/ad/provision-users.sh are reconciled, not re-created, so the
# provisioner-owned weak passwords (the intentional attack surface) are
# preserved. This is a representative subset of the full
# weak/Kerberoastable/over-privileged roster, proving the account-placement
# realization path against real, clean-start-created accounts.
identity_domains:
  techvault:
    profile: active_directory
    dns_name: techvault.local
    netbios_name: TECHVAULT
    authority_account_ref: ad-emily-chen

relationships:
  ad-controls-techvault-domain:
    type: domain_controller_for
    source: ad
    target: techvault
    domain_controller: {}

accounts:
  ad-jessica-williams:
    username: jessica.williams
    node: ad
    groups: [Sales, VPN-Users]
    password_strength: weak
    mail: jessica.williams@techvault.local
  ad-svc-sql:
    username: svc-sql
    node: ad
    password_strength: weak
    spn: MSSQLSvc/db.techvault.local:1433
    domain_ref: techvault
  ad-emily-chen:
    username: emily.chen
    node: ad
    groups: [Engineering, IT-Admins, "Domain Admins"]
    password_strength: medium
    mail: emily.chen@techvault.local
  ad-former-employee:
    username: former.employee
    node: ad
    password_strength: weak
    disabled: false
