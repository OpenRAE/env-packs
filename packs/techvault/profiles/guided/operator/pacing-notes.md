# Facilitator pacing notes

Use the ACES nodes as the shared vocabulary; do not introduce service names or
objectives that are absent from `sdl/techvault.sdl.yaml`.

1. Confirm participant access is through `kali` and the expected three network
   links are present.
2. Let discovery run before directing attention to `webapp`.
3. Pause after the authored `webapp-sqli-login` vulnerability is demonstrated
   and compare Wazuh/Suricata observations.
4. If the group continues into AD, database, workstation, or fileshare work,
   keep prompts evidence-led rather than prescribing an invented sequence.
5. Close with a red/blue timeline and record realization gaps separately from
   scenario-authoring gaps.
