# Wazuh - Filebeat configuration file
# Override of the default filebeat.yml to enable archives indexing.
# The default image ships with archives: enabled: false.
#
# Archives require logall_json=yes in ossec.conf (already configured).
# See: https://documentation.wazuh.com/current/user-manual/manager/wazuh-archives.html
filebeat.modules:
  - module: wazuh
    alerts:
      enabled: true
    archives:
      enabled: true

setup.template.json.enabled: true
setup.template.overwrite: true
setup.template.json.path: '/etc/filebeat/wazuh-template.json'
setup.template.json.name: 'wazuh'
setup.ilm.enabled: false
output.elasticsearch:
  hosts: ['https://wazuh.indexer:9200']
  username: 'admin'
  password: 'SecretPassword'
  ssl.verification_mode: 'full'
  ssl.certificate_authorities: ['/etc/ssl/wazuh/root-ca-manager.pem']
  ssl.certificate: '/etc/ssl/wazuh/wazuh.manager.pem'
  ssl.key: '/etc/ssl/wazuh/wazuh.manager-key.pem'

logging.metrics.enabled: false

seccomp:
  default_action: allow
  syscalls:
  - action: allow
    names:
    - rseq
