# `build/` — golden build (reference)

Scripts that stand up the **golden version of the scenario in AWS**. This is the
reference build a range builder decomposes into their own content system.
**Required-if-present** with the rest of the reference triangle (`tests/` +
`docs/walkthroughs/`).

AWS is assumed for the golden build.
