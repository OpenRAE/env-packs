# `assets/` — bespoke content

The custom-authored files the scenario needs and the SDL references: container
definitions, custom binaries, planted documents, site content, decks, handouts.
**Optional** — include what the scenario has.

Anything generic enough to be shared across scenarios does not belong here; keep
`assets/` to this scenario's bespoke material.
