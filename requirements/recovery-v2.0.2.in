# Historical runtime closure for the final aces-scenario-packs release.
# This is recovery-only input for the signed v2.0.2 target in ADR 0026.
-r build.in
PyYAML>=6
aces-sdl==0.23.1
cyclonedx-bom==7.3.1
# Floor, not a pin: the historical closure pulls cryptography transitively
# (aces-sdl, asyncssh, pyjwt) and the resolver would otherwise hold it at the
# version this lock was first generated with, which is affected by
# GHSA-g6cj-pr64-35w5 (PKCS#7 Bleichenbacher oracle, high).
cryptography>=50.0.0  # GHSA-g6cj-pr64-35w5
